node_modules/

# generated at build time from the workbook - committing it would let the
# published data drift from the spreadsheet it came from
public/data.json

# Excel lock files
~$*.xlsx
.~lock.*
